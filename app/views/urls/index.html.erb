<div class="page-header">
  <h1>Listing urls <small> ( <%= link_to 'Create Url', new_url_path %> ) </small></h1>
</div>

<ol class="breadcrumb">
  <li><a href="/">Home</a></li>
  <li class="active">Urls</li>
</ol>

<table class='table table-striped table-hover'>
  <thead>
    <tr>
      <th>Url</th>
      <th>Status</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @urls.each do |url| %>
      <tr>
        <td><%= url.url %></td>
        <td><%= url.status %></td>
        <td width=30><%= link_to ' ', url, class: 'glyphicon glyphicon-eye-open' %></td>
        <td width=30><%= link_to ' ', edit_url_path(url), class: 'glyphicon glyphicon-edit' %></td>
        <td width=30><%= link_to ' ', url, method: :delete, data: { confirm: 'Are you sure?' }, class: 'glyphicon glyphicon-remove' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= paginate @urls %>